{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start API Test Database",
      "type": "shell",
      "command": "docker compose -f docker-compose.yml -f docker-compose.test-api.yml up -d dbapi",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "silent",
        "panel": "dedicated"
      },
      "runOptions": {
        "runOn": "folderOpen"
      },
      "problemMatcher": []
    }
  ]
}
