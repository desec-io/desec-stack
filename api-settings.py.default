import os
BASE_DIR = os.path.dirname(os.path.dirname(__file__))

DEBUG = False

EMAIL_HOST = 'mail.your.hostname.example.com'
EMAIL_HOST_USER = 'user'
EMAIL_HOST_PASSWORD = 'password'
EMAIL_PORT = '25'

ALLOWED_HOSTS = ['desec.your.hostname.example.com', 'update.dedyn.your.hostname.example.com']

ADMINS = (('John Doe', 'john.doe@example.com'), )

SEPA = {
    'CREDITOR_ID': 'TESTCREDITORID',
}
