#!/usr/bin/env bash
docker-compose -f docker-compose.yml -f docker-compose.tests.api.yml build \
	&& docker-compose -f docker-compose.yml -f docker-compose.tests.api.yml up --abort-on-container-exit ns-tests-api
